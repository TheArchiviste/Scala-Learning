val x = 42
val y = 3 + 4
val z = x / y

// Immutability 
// We cannot reassign values => gives errors
// z = 10

// There is a way to mutate a value, however, used
// in occasional situations only!!!
var z = 9